# 氢哨 H2 Sentinel · 傻瓜式使用说明

> 一句话：这是一个**本地网页应用**——导入电厂数据 → 自动找异常 → 看证据 → 问运维助手 → 导出报告。
> 不需要联网（可选的 AI 润色功能除外），不会碰任何真实设备。

---

## 一、开始前只看这一条

| 你想干什么 | 需要准备 |
|---|---|
| 快速看效果 / 给人演示 | 只需 **Node.js**（第一次用先执行下面"第二步"的装依赖命令） |
| 导入真实数据分析 | 上面 + **Python 3.11+ 和 uv**（本机已装好，不用管） |

命令都在**项目根目录** `D:\allcode\qingneng` 下执行（打开 PowerShell，`cd D:\allcode\qingneng`）。

---

## 二、第一次使用：装依赖（只做一次，约 2 分钟）

```powershell
npm ci
cd services/h2-analytics
uv sync --locked --extra dev
cd ..\..
```

看到没有任何红色 ERROR 就是成功。（本机已经做过这步的话直接跳到第三步。）

---

## 三、启动（二选一）

### 方式 A：演示模式（最简单，内置示例数据，不需要 Python）

```powershell
npm run h2:fixture
```

### 方式 B：完整分析模式（可导入真实 CSV）

```powershell
npm run h2:local
```

**启动成功长这样**：窗口持续滚动日志、不报红字。
**然后打开浏览器**，地址栏输入：**http://127.0.0.1:5173**

> ⚠️ 注意：直接双击 `start-h2-sentinel.bat` 会报错"must be fixture or local"——
> 它必须带参数，例如：`start-h2-sentinel.bat --mode fixture`。**推荐用上面的 npm 命令**。

---

## 四、六个页面怎么用（从左到右点一遍就会了）

侧边栏从上到下依次是：

| 页面 | 干什么 | 傻瓜式操作 |
|---|---|---|
| ① 系统总览 | 一眼看全站状态 | 点"评委黄金路径"步骤条跟着走即可 |
| ② 异常事件中心 | 所有异常的列表 | 用顶部的类别按钮（C01~C07）筛选，点任意一行进详情 |
| ③ 异常诊断详情 | 单个异常的证据链 | 看证据面板（时间/变量/实际值/参考值）→ 看影响 → 看安全建议；底部可"人工确认/驳回/加备注" |
| ④ 数据分析 | 导入自己的 CSV | 点"导入"选 CSV 文件（大文件也支持，会显示进度条）→ 看数据质量结论 → 点变量画趋势图 |
| ⑤ 运维助手 | 问它问题 | 点 Q01~Q10 任意按钮出答案；也可以在输入框**直接打字追问**（如"为什么 C05 要提前预警？"）；答不上来它会明说，不会编 |
| ⑥ 报告中心 | 导出文件 | 点任意报告按钮下载；**submission.csv**（评分提交文件）也在这里导出 |

**看图小抄**：PCC 功率 **正数=向电网送电，负数=从电网取电**；储能 **正数=放电，负数=充电**。所有页面都遵守这个约定，不会变。

---

## 五、导出报告（交付/汇报用）

1. 先在方式 B（local 模式）导入数据并完成分析；
2. 进入 **⑥ 报告中心**；
3. 点需要的按钮：异常报告 / **PCC 合规日报** / **submission.csv**（16 列标准提交格式）等 6 种；
4. 文件自动下载到浏览器默认下载目录。

---

## 六、关闭

在启动的那个窗口按 **Ctrl + C**（可能需要按一两次），窗口关闭即完全退出，不会残留后台进程。

---

## 七、常见问题（对号入座）

| 症状 | 原因与解法 |
|---|---|
| 双击 bat 报 "must be fixture or local" | 正常现象，bat 必须带参数；请改用 `npm run h2:fixture` 或 `npm run h2:local` |
| 提示端口被占用（5173/8765） | 换端口：`npm run h2:local -- --web-port 5183 --analytics-port 8775`，浏览器改开 :5183 |
| `npm run` 报 npm 版本问题 | 需 npm 11：`npm install -g npm@11` |
| local 模式起不来 | 先跑体检：`node scripts/h2-sentinel/doctor.mjs --mode local`，按中文提示逐条处理 |
| 浏览器打开是空白/报模式错误 | 地址必须是 `http://127.0.0.1:5173`（不要用别的端口却开 5173） |
| 想开 AI 润色回答（可选） | 启动前设三个环境变量：`STEPFUN_API_KEY`（你的 key）、`H2_LLM_ENABLED=true`、`H2_LLM_MODEL=模型名`。**不开也完全不影响使用**（自动走本地回答） |
| 想开 ML 机器学习校验层（可选） | 设 `H2_ML_ENABLED=true` 后按方式 B 启动；默认关闭，关闭即纯规则模式 |

---

## 八、进阶（一般用不到）

| 命令 | 作用 |
|---|---|
| `node scripts/h2-sentinel/doctor.mjs --mode local` | 环境体检（中文提示） |
| `node scripts/h2-sentinel/check-all.mjs` | 一键跑全部质量门禁 |
| `npm run h2:smoke` | 快速冒烟自检 |

---

## 九、安全边界（一句话版本）

氢哨只**看数据、出诊断、给建议**——不下发任何设备控制指令；每条建议都标了"需人工确认"；最终决定永远由人来做。
