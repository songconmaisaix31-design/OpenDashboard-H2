# C 线任务卡全集（TASKS）

> 版本：2026-08-30 ｜ 配套 `README.md`（白名单/命令/红线）+ `../CONTRACTS.md` + `../COORDINATION.md`
>
> **卡片格式**（四线统一）：目标 / 背景 / 改动文件 / 实现要点 / 验收（命令·方式）/ 服务验收条款 / 会话数 / 依赖 / 裁剪位 / 风险回退。粒度 = 1 个 AI 会话完成（0.25-1d；>1d 拆会话标注）。
>
> **状态取值**：未开始 / 进行中 / 已完成 / 已裁剪（须附理由）。

---

## 状态看板（每卡收工必更）

| 编号 | 任务 | 状态 | 门禁/证据 | 会话 |
|---|---|---|---|---|
| C-P0-1 | 18 分表逐项审计 | 已完成 | 审计表全行有结论 + 截图引用：ACCEPTANCE_AUDIT 六行+汇总全填（2026-08-30 fixture 实测）；证据 7 截图+note.md 入 `evidence/C-P0-1/` | 1 |
| C-P0-2 | 总览 KPI 完整性（4 分） | 未开始 | 六要素逐项截图 + fixture 冒烟 + 审计表转绿 | 1 |
| C-P0-3 | 事件中心联动补强（4 分） | 未开始 | 手动验收清单 + 截图 + 审计表转绿 | 2 |
| C-P1-1 | 六页中文名/单位一致性 | 未开始 | 30 项抽查零偏差清单 + 缺口登记 | 1 |
| C-P1-2 | 演示脚本 v2 | 未开始 | run-demo 双次 <180s + 脚本逐屏核对 | 1 |
| C-P1-3 | 报告导出打磨（2 分） | 未开始 | 两类导出样例留痕 + 现场可复现 | 2 |
| C-P1-4 | 企业价值物料包 | 未开始 | 3 件物料 v1 + 内部评审 + 数字可溯源 | 2 |
| C-P2-1 | 分析/诊断页残余打磨 | 未开始 | 截图对比（前后） | 1 |

## 普遍原则（每卡默认适用，不再逐卡重复）

- **少测试快交付**：手动验收清单 + 截图留痕为主；不新增自动化测试；确需断言 <100 行/任务；不引新框架；不破坏既有门禁（收工跑 `npm run h2:check`；动 `renderer.py` 加跑相关 pytest）。
- 截图与清单入 `plan0830/C/evidence/<任务ID>/`；命名与记录规则见 `README.md` §5。
- 跨线需求一律 `../CONTRACTS.md` change-request：B=助手呈现/Q09/Q10 口径、A=数值口径、D=启动器/submission/截图晋升。
- 每卡一个独立 commit（限本线域内）；commit 前更新看板；commit message 带 `C-Px-y` 编号。

---

## C-P0-1 · 18 分表逐项审计（0.5d / 1 会话）

**目标**：将 Web 验收 18 分表 6 行（5 个有分值验收项 + 1 个访问方式门槛项）逐项过现状 → 差距 → 负责任务 ID，产出并填实 `plan0830/C/ACCEPTANCE_AUDIT.md`（全周知共享工件，C 线持笔）。

**背景**：18 分表是唯一可见评分（需求书缺第 8-10、13 节，算法分值不可见）；防御性评分设计的锚点。G1 门禁要求"审计表全行有结论"；本卡是 C 线首卡，也是全四线的评分地图。

**改动文件**：`plan0830/C/ACCEPTANCE_AUDIT.md`（开档填实）、`plan0830/C/evidence/C-P0-1/**`（截图与走查记录）。

**实现要点**：
1. `npm run h2:fixture` 启动，逐页走查六页；对 18 分表 #1/#2/#4 三行（C 线直接持分行）逐项截图留痕。
2. 每行填四列：现状评估（写实，引用具体页面/文件）/ 差距 / 负责任务 ID（本线卡或跨线卡，跨线行注明主责线）/ 证据与截图引用。
3. #3 运维助手行与 #5 部署行：C 线只从**演示呈现角度**记录现状（能否演、怎么演），内容/合规主责归 B/D 线，标注"待 B/D 线补充"。
4. #0 访问方式行（门槛无分值）：记录使用说明与启动命令现状，差距指向 D-P0-1。
5. 现状初判起点可引用 `plan0829/01_GAP_ANALYSIS.md` §1（2026-08-29 基线），但必须以本次 fixture 实测复核，不照抄。

**验收**：审计表全行有结论（现状列非空）+ C 线三行有截图引用；`../00_README.md` 看板同步本卡完成。

**服务条款**：18 分表全项视角；验收-T09/T10/T12 呈现面。

**会话数**：1（0.5d）。

**依赖**：无（本线首卡）。

**裁剪位**：不可裁（P0；全线路径图）。

**风险回退**：若 fixture 启动失败 → 先向 D 线报 launch 问题（不自行改 `scripts/`），暂以书面审计 + `SCREENSHOT_SHOT_LIST.md` 状态列先填，UI 复核后补截图。

---

## C-P0-2 · 总览 KPI 完整性（4 分）（1d / 1 会话）

**目标**：总览页 KPI 六要素齐备且数值真实——**光伏、储能、PCC、配额、电解槽和异常 KPI**（18 分表原文），fixture 与 local 双模式一致呈现。

**背景**：18 分表 #1「系统总览」4 分，验收方式=**页面检查**（评委打开即见，无操作深度要求，拼的是首屏完整与准确）。plan0829 基线：6 项 KPI + 黄金路径 + C03/C04 案例卡已齐备，但双模式逐项一致性未核验、无成套留痕截图。

**改动文件**：`apps/web/src/features/h2-sentinel/pages/overview/OverviewPage.tsx`、`model/presentation.ts`（如需 KPI 定义补齐；单源纪律）。

**实现要点**：
1. 对照审计表 #1 行逐要素（光伏/储能/PCC/配额/电解槽/异常）截图：fixture 与 local 各一套，共 ≥12 张。
2. KPI 中文名/单位走 `presentation.ts` 单源（源自 `fields.json`），本卡内发现的不一致登记并就地修正（大范围修正归 C-P1-1）。
3. 异常 KPI 计数来源 = 分析侧 API 契约（`api/models.py` 视图），前端不自算、不用报警计数代替。
4. 配额类 KPI 展示日累计与限值口径（4500/20000kWh），常数取自 `constraints.json` 只读文件或契约，禁止硬编码。
5. 演示黄金路径（KPI → 下钻事件）保持可走通；为 C-P1-2 演示脚本标记各 KPI 的推荐停留点（注释即可，不写死文案）。

**验收**：六要素逐项截图（fixture+local 双模式）+ `npm run h2:fixture` 冒烟通过 + 审计表 #1 行转绿（证据列填截图编号）。

**服务条款**：验收-T09 Web 应用、验收-T10 可视化与事件交互。

**会话数**：1（1d）。

**依赖**：C-P0-1。

**裁剪位**：不可裁（P0；直接持 4 分）。

**风险回退**：KPI 数值口径疑问 → change-request 问 A 线，不代改；UI 改动单 commit，`git revert` 即回 plan0829 基线。

---

## C-P0-3 · 事件中心联动补强（4 分）（1.5d / 2 会话）

**目标**：事件中心四要素——**筛选、排序、事件详情和联动曲线**（18 分表原文）全达标：筛选（类别/严重度/设备/时间）、排序（时间/严重度）、事件详情（证据变量+时间窗+实际/参考值）、联动曲线（选中事件 → 相关变量曲线 + **事件窗底纹** + **诊断页跳转**）。

**背景**：18 分表 #2「事件中心」4 分，验收方式=**页面操作**（评委会动手点）。plan0829 差距：仅 C03 有专属图表序列（`chart-options.ts`），其余类别走"前 5 证据变量"降级；联动深度不均衡是 4 分主要失分风险。

**改动文件**：`pages/events/EventsPage.tsx`、`pages/diagnosis/DiagnosisPage.tsx`、`model/chart-options.ts`。

**实现要点**：
1. 联动曲线最低标准：任一类别事件选中后，图表以**事件窗底纹（markArea）**标出窗口 + 展示该事件证据变量序列；时间轴与诊断/分析页一致（红线 #1）。
2. 诊断页跳转携带事件标识，落地即定位到该事件证据链；返回事件中心不丢筛选状态。
3. 筛选/排序组合至少覆盖：类别×严重度、设备×时间两族；空结果给显式空态（不白屏）。
4. 序列配置经 `chart-options.ts` 统一注册，变量名/单位走单源；某类证据变量在 fixture 数据缺列 → 如实展示缺省说明，**不造假序列**。
5. 手动验收清单覆盖 C01-C07 中至少 4 类事件 ×（筛选+排序+详情+联动）各 ≥2 例。

**验收**：手动验收清单（存 `evidence/C-P0-3/checklist.md`）+ 全部截图 + 审计表 #2 行转绿 + `npm run h2:test` 不红。

**服务条款**：验收-T10 可视化与事件交互、验收-T09。

**会话数**：2（1.5d：会话 1=联动曲线+底纹；会话 2=筛选/排序补强+清单走查）。

**依赖**：C-P0-1；与 C-P0-2 并行开工，若同文件冲突先让 C-P0-2。

**裁剪位**：不可裁（P0；直接持 4 分）。

**风险回退**：图表序列扩展导致 fixture 缺列报错 → 回退到"前 5 证据变量"降级路径（plan0829 现状），保底不弱于基线；单 commit revert。

---

## C-P1-1 · 六页中文名/单位一致性（1d / 1 会话）

**目标**：六页所有变量中文名/单位与 `fields.json` 单源**零偏差**；设备名与台账（PV01/BESS01/PCC01/EMS01/ELZ01-03/AUX01）一致。

**背景**：需求书明文：中文名/单位以变量字典为准、跨页一致；设备名与台账一致。属验收-T09/T10 的合规细节分，也是评委翻页时的直接观感项。

**改动文件**：`model/presentation.ts`（主战场）+ 六页零散文案修正（`pages/**`）。

**实现要点**：
1. 产出 **30 项抽查零偏差清单**：覆盖六页 × 高频变量（光伏出力/储能 SOC 与充放/PCC 功率与累计电量/配额与剩余/电解槽三台出力与 run_state/异常 KPI 等），方法 = UI 实际文案 vs `fields.json` 逐项对照表。
2. 偏差就地修正（一律改 `presentation.ts` 或引用处，**不改 fields.json**）。
3. **缺口（字典未覆盖项）走 change-request** 登记，不自行造词；UI 暂用现文案并在清单标注"待字典补录"。
4. 设备名 8 台账名逐页核对（含下拉/筛选器/图例/诊断归因文案）。

**验收**：30 项抽查零偏差清单落档（`evidence/C-P1-1/checklist.md`，偏差修正前后对照）；缺口全部登记 change-request；`npm run h2:check` 绿。

**服务条款**：验收-T09、验收-T10。

**会话数**：1（1d）。

**依赖**：无硬依赖；建议在 C-P0-2/C-P0-3 合并后做，避免重复走查。

**裁剪位**：裁剪序第 2 —— 缩为总览/事件两页抽查（两页是 8 分所在）。

**风险回退**：纯文案层改动，单 commit 可整体回退。

---

## C-P1-2 · 演示脚本 v2（1d / 1 会话）

**目标**：`DEMO_SCRIPT.md` 升 v2：**3 分钟核心版 + 5 分钟全流程版 + 现场问答段 + 离线降级段**，与 `validation/run-demo.mjs` 对齐（双次 <180s）。

**背景**：18 分表 #3 运维助手 4 分的验收方式是**现场问答**——助手是"演示即评分"；#5 部署 4 分亦靠演示流畅度呈现。plan0829 已固化 run-demo 双次 <180s，但脚本 v1 未含问答段与降级段、未与六页 KPI 补强后的新状态对齐。

**改动文件**：`submission/h2-sentinel/DEMO_SCRIPT.md`、`submission/h2-sentinel/JUDGE_CHECKLIST.md`。

**实现要点**：
1. **3 分钟核心版**：启动 → 总览六 KPI（对应 C-P0-2）→ 事件中心四要素操作（对应 C-P0-3）→ 1 个诊断证据链下钻 → 1 次报告导出。
2. **5 分钟全流程版**：3 分钟版 + 分析页多变量趋势 + 助手 Q01-Q10 挑 3 题现场问答 + 复核（确认/驳回）流。
3. **现场问答段**：题目清单与 B-P0-3 的 Q 强化口径对齐（引用其问答设计，不代写答案内容）；标注每题预期回答要素（真实数值/引用/三段式）。
4. **离线降级段**：LLM 不可用时的三态切换演示动作（口径归 B-P0-2，本卡只写演示步骤与预期 UI 状态，含 FIXTURE/降级标签如实声明）。
5. 每步标注：预期画面/停留秒数/截图编号；步骤顺序与 `run-demo.mjs` 对齐，收工跑双次 <180s 复核。
6. `JUDGE_CHECKLIST.md` 同步：评委视角逐项打勾点与 18 分表行号对应。

**验收**：`node validation/run-demo.mjs` 双次 <180s；脚本逐屏与 UI 核对（每步可复现、无虚构按钮）；JUDGE_CHECKLIST 同步更新。

**服务条款**：验收-T09；18 分表 #2/#3 演示呈现（支撑 #3 现场问答 4 分与 #5 换机演示）。

**会话数**：1（1d）。

**依赖**：C-P0-2、C-P0-3（R2 执行）。

**裁剪位**：不在预定义裁剪序（裁剪链仅 C-P2-1→C-P1-1→C-P1-4）；若裁，沿用 plan0829 v1 脚本（git 历史可回）。

**风险回退**：`git revert` 回 v1；v1 已过双次 <180s 门禁，无评分回退风险。

---

## C-P1-3 · 报告导出打磨（2 分）（1.5d / 2 会话）

**目标**：**异常报告 + PCC 合规日报**两类导出（18 分表 #4 原文口径）内容/格式/命名规范达标，现场可复现。

**背景**：18 分表 #4「报告」2 分，验收方式=**导出文件**（评委拿文件看）。plan0829 差距：模板美化与 PCC 日报内容完整性待复核。renderer 已升 v2（StepFun 收尾件），本卡做内容与规范层。

**改动文件**：`services/h2-analytics/src/h2_analytics/reports/renderer.py`、`pages/reports/ReportsPage.tsx`、`model/reporting.ts`。

**实现要点**：
1. 异常报告：中文分节/证据表（变量+时间窗+实际值/参考值）/影响口径/安全声明（人工确认）格式统一；带 provenance（数据切片+哈希）。
2. PCC 合规日报：日窗口口径、配额消耗进度、越限/接近越限记录完整性三块齐备；数值与总览页 KPI 同源同值（红线 #1/#2 引申）。
3. 文件命名规范：含报告类型+时间戳+模式标签（fixture 产物必须可辨识，不与 official 切片混淆）。
4. **Q09/Q10 口径经 `../CONTRACTS.md` 对接 B 线**（助手引用报告数据时的取数与表述一致）；C 不改 assistant 侧文件。
5. 现场复现步骤写入 DEMO_SCRIPT（与 C-P1-2 互引）。

**验收**：两类导出样例留痕（fixture 模式生成，文件+截图入 `evidence/C-P1-3/`）；现场复现步骤可照做；审计表 #4 行转绿；renderer 相关 pytest 不红。

**服务条款**：验收-T12 报告与导出。

**会话数**：2（1.5d：会话 1=renderer 内容层；会话 2=前端装配+复现走查）。

**依赖**：无硬依赖（R2 执行；与 B 线 Q09/Q10 联动处走 change-request）。

**裁剪位**：不在预定义裁剪序（裁剪链仅 C-P2-1→C-P1-1→C-P1-4，见 `README.md` §6）；若 R2 超支由整合窗决策降级，保底=现状 6 种导出不动（2 分项维持 plan0829 水平）。

**风险回退**：`renderer.py` 单 commit revert，不影响 `submission.py`（D 线域）；`ReportsPage`/`reporting.ts` 独立 commit。

---

## C-P1-4 · 企业价值物料包（1.5d / 2 会话）

**目标**：三件企业价值物料 v1：**价值简报**（130+ 提交演进 + 0829→0830 增量叙事）、**答辩 Q&A 预案**（企业 10 题 + 评委常见问）、**演进对比一页纸**。

**背景**：用户决策「让企业评委看到价值与用心」；plan0830 相对 plan0829 的增量（A 先验融合 / B 语料与三态 / C 演示补强 / D 换机合规）需要成叙事，而非散在代码里。素材源 = `claudedocs/项目与云端原始版优化对比.md` + plan0829 全档 + `submission/h2-sentinel/CLAIMS_LEDGER.md`。

**改动文件**：`submission/h2-sentinel/TEN_PAGE_PROJECT_NARRATIVE.md`（更新增量节）、`LEIDONG_ENTERPRISE_ALIGNMENT_BRIEF.md`（更新）、`答辩QA.md`（新建，`submission/h2-sentinel/` 下）。

**实现要点**：
1. **价值简报**：叙事线 = 130+ 提交演进（数字以 `git log` 终值复核为准）→ 0829 冻结基线（F1=0.9718 / plan0829 终局冻结 gate-s6 / 六页双模式 / 演示双次 <180s）→ 0830 增量（四线各自一句话+一个可演示证据点）。
2. **答辩 Q&A 预案**：企业 10 题承 `plan0829/07_APPENDIX_ENTERPRISE_QUESTIONS.md`，口径与 D 线外联台账（`../D/ENTERPRISE_OUTREACH.md`）**一致不矛盾**（未获答复的题按保守默认口径作答）；评委常见问（数据来源/红线合规/可复现性/ML 可解释性/fixture 声明）各备 3 句内答法。
3. **演进对比一页纸**：前后对比表（指标 / 功能 / 交付三维，0829 vs 0830 终值）。
4. 所有数字可溯源（CLAIMS_LEDGER 口径 + git log/验证输出双源核对），**不造不可复现的数字**；R2 功能冻结后取终值（初稿可 R2 中段先写，终值字段留占位）。

**验收**：3 件物料 v1 + 内部评审通过（交 team-lead 过目）+ 数字溯源清单（每数字注出处）。

**服务条款**：企业价值呈现（非直接条款分；支撑验收-T14 合规形象与答辩环节）。

**会话数**：2（1.5d：会话 1=简报+一页纸初稿；会话 2=Q&A 预案+终值回填）。

**依赖**：R2 功能冻结后取终值（G2 前完成 v1）。

**裁剪位**：裁剪序第 5 —— 降为演进对比一页纸。

**风险回退**：纯文档无代码风险；口径风险=数字必须双源核对，发现矛盾以 CLAIMS_LEDGER/验证输出为准并修正。

---

## C-P2-1 · 分析/诊断页残余打磨（1d / 1 会话，可裁）

**目标**：分析页/诊断页在六类图表主线之外的体验残余收口：状态矩阵（loading/disabled/error/conflict）、窄宽度（390x844）主流程不裁切、长文本溢出。

**背景**：`SCREENSHOT_SHOT_LIST.md` S10（窄宽度状态矩阵不完整）与 S15（缺列降级证据）提示的残余面；P0/P1 完成后的扫尾卡。

**改动文件**：`pages/analysis/AnalysisPage.tsx`、`pages/diagnosis/DiagnosisPage.tsx`、`components/charts/EChartsCanvas.tsx`（如需）。

**实现要点**：
1. 按 S10 口径补状态矩阵截图（loading/disabled/error/conflict 四态 × 主流程）。
2. 390x844 视口过总览→事件→诊断主流程，裁切/重叠/隐藏动作逐项修。
3. 只修呈现层，不动数据链路与口径。

**验收**：前后截图对比（`evidence/C-P2-1/`）+ `npm run h2:check` 绿。

**服务条款**：验收-T10。

**会话数**：1（1d）。

**依赖**：无（建议最后做）。

**裁剪位**：裁剪序第 1（首先裁）。

**风险回退**：单 commit revert。

---

## 收尾核对（本线 G2 自检）

- [ ] 审计表 6 行全绿或差距明示（含跨线行回填）
- [ ] 截图晋升 `submission/h2-sentinel/screenshots/` 并对齐 S01-S15 编号
- [ ] DEMO_SCRIPT v2 + JUDGE_CHECKLIST 与最终 UI 一致；run-demo 双次 <180s
- [ ] 3 件物料 v1 终值回填完毕
- [ ] `npm run h2:check` 全绿；`renderer.py` 相关 pytest 绿
- [ ] 看板 8 行终态（已完成/已裁剪+理由）同步 `../00_README.md`
