# C-P0-3 手动验收清单（会话 1 联动曲线+底纹 ｜ 会话 2 排序+组合）

> 范围：任务卡 C-P0-3 全卡（联动曲线+底纹+筛选态保持〔会话 1〕；排序控件+组合走查〔会话 2〕）。
> 日期：2026-08-30 ｜ 模式：fixture ｜ 命令：`npm run h2:fixture`（web 5173）｜ 视口：1440×900（fullPage 截图为全页）｜ 工具：Playwright MCP

## 会话 2 用例（排序+组合，2026-08-30 追加）

| 用例 | 页面 | 模式 | 操作步骤 | 预期 | 实际 | 截图编号 | 结论 |
|---|---|---|---|---|---|---|---|
| S1 | 事件中心 | fixture | 打开页面不操作（默认排序） | 默认 startTime 降序（最新在前），排序控件显示「开始时间 ↓ 降序」 | C04(18:32)→C03(18:20)，控件与 aria-label 齐 | —（DOM 序断言） | 通过 |
| S2 | 事件中心 | fixture | 点击方向按钮 | 升降切换，顺序反转，按钮文案/aria-label 同步 | C03→C04，按钮变「↑ 升序」 | 05 | 通过 |
| S3 | 事件中心 | fixture | 排序字段切「置信度」（保持升序） | 按置信度升序 | C04(91%)→C03(94%) | —（DOM 序断言） | 通过 |
| S4 | 事件中心 | fixture | 排序字段切「严重度」 | 按严重度排序；同级稳定（不乱序） | 两事件同为高风险，顺序保持稳定 | —（DOM 序断言） | 通过 |
| S5a | 事件中心 | fixture | 组合族① 设备×时间：设备输入 BESS + 排序 startTime 升 | 筛选缩集后排序正常作用 | BESS→1/2 仅 C03；（设备 PCC 时 2/2——C03 证据含 PCC 设备，符合契约非 bug） | 06（组合态近邻） | 通过 |
| S5b | 事件中心 | fixture | 组合族② 类别×严重度：类别 C04 + 严重度 高 | 交集筛选 | 1/2 仅 C04，排序仍生效 | 06 | 通过 |
| S6 | 事件中心→诊断→返回 | fixture | 设 code=C04 + sort=置信度升序 → 行内「→」跳诊断 → 侧栏返回（SPA 内部导航） | 筛选与排序**双保留** | 返回后 code=C04、排序=confidence ↑ 升序、计数 1/2 全保留 | 07 | 通过 |
| S7 | 单元/类型 | — | `npm run h2:test`；`npm run typecheck` | 138/138 绿；typecheck 无错 | 全绿 | —（命令输出） | 通过 |

## 覆盖口径说明（≥4 类事件清单的处理）

- 18 分表 #2 验收方式=页面操作（筛选/排序/详情/联动四要素）。本 fixture run 仅含 C03/C04 两事件，四要素 × 两事件全走查通过（会话 1 L1-L7 + 会话 2 S1-S7）。
- C01/C02/C05/C06/C07 五类联动序列为 `chart-options.ts` 代码级齐备（专属序列定义 + 缺列降级前 5 证据变量路径），结构上与 C03/C04 同一装配管线（`getEventSeriesDefinitions` → `createEventChartOption`）；逐类 UI 实测待 local 模式导入含该类事件的切片后补（登记于 AUDIT#2 差距列）。


| 用例 | 页面 | 模式 | 操作步骤 | 预期 | 实际 | 截图编号 | 结论 |
|---|---|---|---|---|---|---|---|
| L1 | 事件中心 | fixture | 打开 `#h2/events`，不操作 | 默认选中事件（App 层默认 C03）行高亮 + 页内联动曲线面板出现，含事件窗底纹 | 默认选中 C03，面板标题「联动曲线 · C03 储能充放电方向异常」，三序列（调度指令虚线/储能实际功率/并网点功率）+ 淡蓝底纹 + 「查看完整证据链 →」 | 01 / 02 | 通过 |
| L2 | 事件中心 | fixture | 点击 C04 行 | 行选中态切换至 C04；联动面板即时切换为 C04 专属序列（并网点实际功率 + 送出边界 500kW 虚线 + 受电边界），时间轴=事件窗 18:32–18:40 | 全部符合；C04 行淡蓝高亮 | 04 | 通过 |
| L3 | 事件中心 | fixture | 筛选「异常类型=C03」（此时选中为 C04） | 计数 1/2；所选 C04 不在筛选结果内 → 联动面板出现诚实提示「当前筛选未包含所选事件；曲线仍显示所选事件，未替换为其他数据」 | 计数 1/2 + 提示出现，曲线仍为 C04（未偷换数据） | 03 | 通过 |
| L4 | 事件中心→诊断→返回 | fixture | L3 状态下点「查看完整证据链 →」，再从侧栏返回「异常事件」 | 诊断页落地携带事件标识（URL `#h2/diagnosis/C04-20260105-001`）；返回后筛选（C03）与选中（C04）均保留 | URL 携带 eventId；返回后筛选仍 C03（下拉 selected）、计数仍 1/2、联动仍 C04 + 提示仍在 | 03 | 通过 |
| L5 | 事件中心 | fixture | 行选中驱动方式=鼠标点击；键盘路径=行 tabIndex 聚焦 + Enter | 行 cursor:pointer、focus-visible 描边；Enter/Space 选中（onKeyDown） | 实现于 EventsPage（tr tabIndex=0 + keyDown 处理 + `aria-selected`）；a11y 快照确认 C03 行 `[selected]` 状态暴露 | —（代码+a11y 快照） | 通过 |
| L6 | 事件中心 | fixture | 时间轴一致性（红线 #1） | 联动图与诊断页同一查询构造器（`createH2DiagnosisSeriesQuery`，事件窗）+ 同一图表装配（`createEventChartOption`，markArea 底纹），不得自算时间轴 | 复用同函数未新增时间轴逻辑；`requestH2Series` 契约校验强制所有点落在事件窗内 | —（代码级） | 通过 |
| L7 | 事件中心 | fixture | canvas 渲染完整性 | 图表非空白（折线/图例/底纹/轴刻度齐备） | canvas 像素采样非空白占比 65.2%（1454×352）；视觉核对 02/04 全页截图序列与底纹齐备 | 02 / 04 | 通过 |
| L8 | 单元测试 | — | `npm run h2:test`；`npm run typecheck` | 138/138 绿；typecheck 无错 | 138 pass / 0 fail；typecheck 通过 | —（命令输出） | 通过 |

## 会话 1 未覆盖（转会话 2）

- 排序控件（18 分表 #2 四要素中唯一 ❌ 项，AUDIT#2 已记录）。
- 筛选/排序组合族（类别×严重度、设备×时间）≥2 例走查与空态复核。
- C01–C07 中 ≥4 类事件 ×（筛选+排序+详情+联动）完整清单（fixture 本 run 仅 C03/C04 两事件，其余类别联动序列为代码级齐备 `chart-options.ts` C01-C07 专属定义 + 缺列降级路径，逐类实测需 local 导入更全切片或转会话 2 说明）。
